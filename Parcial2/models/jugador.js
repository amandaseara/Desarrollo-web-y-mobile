class jugador {

    constructor(id, name, position, suspended, inyured) {
      this.id = id;
      this.name = name;
      this.position = position;
      this.suspended = suspended;
      this.inyured = inyured;
    }

}